{% extends 'base.html.twig' %}

{% block title %}Rappels SMS - OpsTracker{% endblock %}

{% block body %}
<div class="min-h-screen flex flex-col">
    {# HEADER PUBLIC #}
    <header class="bg-ink text-white px-8 py-6">
        <div class="max-w-4xl mx-auto">
            <div class="flex items-center justify-between">
                <div>
                    <span class="text-xs font-semibold uppercase tracking-wider text-white/60">OpsTracker</span>
                    <h1 class="text-xl font-bold">Notifications SMS</h1>
                </div>
                <div class="text-right">
                    <span class="text-sm text-white/60">Bonjour,</span>
                    <p class="font-semibold">{{ agent.nomComplet }}</p>
                </div>
            </div>
        </div>
    </header>

    {# MAIN CONTENT #}
    <main class="flex-1 px-8 py-10">
        <div class="max-w-2xl mx-auto">

            {# Flash messages #}
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="mb-6 p-4 border-2 border-ink/20 bg-{{ label == 'success' ? 'success' : (label == 'warning' ? 'warning' : (label == 'info' ? 'primary' : 'danger')) }}/10 flex items-center gap-3">
                        <i data-feather="{{ label == 'success' ? 'check-circle' : (label == 'warning' ? 'alert-triangle' : (label == 'info' ? 'info' : 'alert-circle')) }}" class="w-5 h-5 text-{{ label == 'success' ? 'success' : (label == 'warning' ? 'warning' : (label == 'info' ? 'primary' : 'danger')) }}"></i>
                        <span class="text-sm font-medium">{{ message }}</span>
                    </div>
                {% endfor %}
            {% endfor %}

            {# INFO BOX #}
            <div class="bg-primary/10 border-2 border-primary p-6 mb-8">
                <div class="flex items-start gap-4">
                    <div class="w-10 h-10 bg-primary/20 flex items-center justify-center shrink-0">
                        <i data-feather="smartphone" class="w-5 h-5 text-primary"></i>
                    </div>
                    <div>
                        <h2 class="text-lg font-bold text-ink mb-2">Rappels par SMS</h2>
                        <p class="text-sm text-muted">
                            Recevez un rappel SMS la veille de votre rendez-vous pour ne pas oublier votre creneau.
                            Ce service est optionnel et vous pouvez le desactiver a tout moment.
                        </p>
                    </div>
                </div>
            </div>

            {# FORMULAIRE #}
            <form action="{{ path('app_booking_sms_optin', {token: token}) }}" method="POST" class="bg-white border-2 border-ink p-8">
                <input type="hidden" name="_token" value="{{ csrf_token('sms_optin') }}">

                <div class="space-y-6">
                    {# TELEPHONE #}
                    <div>
                        <label for="telephone" class="block text-sm font-semibold text-ink mb-2">
                            Numero de telephone mobile
                        </label>
                        <input
                            type="tel"
                            id="telephone"
                            name="telephone"
                            value="{{ agent.telephone }}"
                            placeholder="06 XX XX XX XX"
                            pattern="[0-9+\s\-\.]{10,20}"
                            class="w-full px-4 py-3 border-2 border-ink/20 focus:border-primary focus:outline-none text-ink"
                        >
                        <p class="mt-2 text-xs text-muted">
                            Format accepte : 06 XX XX XX XX ou +33 6 XX XX XX XX
                        </p>
                    </div>

                    {# OPT-IN #}
                    <div class="border-2 border-ink/10 p-4">
                        <label class="flex items-start gap-4 cursor-pointer">
                            <input
                                type="checkbox"
                                name="sms_optin"
                                value="1"
                                {{ agent.smsOptIn ? 'checked' : '' }}
                                class="w-6 h-6 border-2 border-ink mt-0.5 text-primary focus:ring-primary"
                            >
                            <div>
                                <span class="block text-sm font-semibold text-ink">
                                    Je souhaite recevoir un rappel SMS
                                </span>
                                <span class="block text-xs text-muted mt-1">
                                    Vous recevrez un SMS la veille de votre rendez-vous a 18h.
                                </span>
                            </div>
                        </label>
                    </div>

                    {# STATUT ACTUEL #}
                    <div class="p-4 bg-paper border border-ink/10">
                        <p class="text-xs font-semibold text-muted uppercase tracking-wider mb-2">Statut actuel</p>
                        {% if agent.canReceiveSms %}
                            <p class="flex items-center gap-2 text-sm text-success font-medium">
                                <i data-feather="check-circle" class="w-4 h-4"></i>
                                Vous recevrez les rappels SMS
                            </p>
                        {% elseif agent.smsOptIn and not agent.telephone %}
                            <p class="flex items-center gap-2 text-sm text-warning font-medium">
                                <i data-feather="alert-triangle" class="w-4 h-4"></i>
                                Numero de telephone requis pour activer les SMS
                            </p>
                        {% else %}
                            <p class="flex items-center gap-2 text-sm text-muted font-medium">
                                <i data-feather="bell-off" class="w-4 h-4"></i>
                                Rappels SMS desactives
                            </p>
                        {% endif %}
                    </div>

                    {# SUBMIT #}
                    <div class="flex flex-col sm:flex-row items-center gap-4 pt-4">
                        <button type="submit" class="w-full sm:w-auto flex items-center justify-center gap-2 px-6 py-3 text-sm font-semibold text-white bg-primary hover:bg-primary/90 transition-colors">
                            <i data-feather="save" class="w-4 h-4"></i>
                            Enregistrer mes preferences
                        </button>
                        <a href="{{ path('app_booking_index', {token: token}) }}" class="w-full sm:w-auto flex items-center justify-center gap-2 px-6 py-3 text-sm font-semibold text-ink border-2 border-ink/30 hover:border-ink transition-colors">
                            Retour
                        </a>
                    </div>
                </div>
            </form>

            {# INFORMATION RGPD #}
            <div class="mt-8 p-4 bg-paper text-center">
                <p class="text-xs text-muted">
                    <i data-feather="shield" class="w-3 h-3 inline-block align-middle mr-1"></i>
                    Votre numero de telephone est utilise uniquement pour les rappels de rendez-vous.
                    Conformement au RGPD, vous pouvez modifier ou supprimer ces donnees a tout moment.
                </p>
            </div>

        </div>
    </main>

    {# FOOTER #}
    <footer class="bg-paper border-t-2 border-ink/10 px-8 py-6">
        <div class="max-w-4xl mx-auto text-center text-sm text-muted">
            <p>OpsTracker - Gestion des interventions</p>
        </div>
    </footer>
</div>
{% endblock %}
